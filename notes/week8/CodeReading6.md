在 `notes/week8/` 下写 `CodeReading6.md`，回答：
- Agent 和 Server 之间用的是什么协议？数据是什么格式？
-通信协议：TCP   数据格式：Protobuf 序列化后的二进制字节流
- 数据为什么要序列化？直接传 Java 对象行不行？ 
- 不行。因为网络只能传输字节流，Java 对象必须先序列化成字节才能通过 TCP 发送；
- 服务器收到后再反序列化恢复对象。使用 Protobuf 可以提高传输效率，并支持跨语言通信